import React from "react"

const Guilds = () => {
    /**
     * TODO: A page where you can search guilds. Contains stats of what guilds are on each server and how many members they have.
     * Guild activity. And if you have a registered, verified character, you can see all of the members in that character's guild.
     */
    return <div>Guilds</div>
}

export default Guilds
